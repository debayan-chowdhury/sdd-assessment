# Project Status Board

_Last updated: 2026-09-04_

## Active Specs

| Spec ID | Title | Status | Owner | Last Updated | Notes |
|---|---|---|---|---|---|
| admin-static-login | Static Admin Login | In Development | developer.debayan@gmail.com | 2026-09-03 | 4/4 tasks implemented, all tests passing. JWT (8h expiry, still an unbacked assumption). Awaiting Gate 2 code review. |
| location-crud | Location CRUD | In Development | developer.debayan@gmail.com | 2026-09-04 | 10/10 tasks implemented (T09-T10: real hard-delete added). All tests passing. Awaiting Gate 2 code review. |
| department-crud | Department CRUD | In Development | developer.debayan@gmail.com | 2026-09-04 | 10/10 tasks implemented (T09-T10: real hard-delete added). All tests passing. Awaiting Gate 2 code review. |
| role-crud | Role CRUD | In Development | developer.debayan@gmail.com | 2026-09-04 | v1.1 implemented: `category` enum widened (T07). T08-T09: real hard-delete added. All tests passing. Awaiting Gate 2 code review. |
| employee-crud-mapping | Employee CRUD and Manager/HR Mapping | In Development | developer.debayan@gmail.com | 2026-09-04 | v1.3 implemented: `code` removed, `email` + Admin-set `password` replace it (`temporaryPassword` mechanism removed) (T14-T16). T17-T18: real hard-delete added (`EMPLOYEE_HAS_DEPENDENTS` guard). v1.4 (T19): Role/Department enablement check removed from create/update — no relation between the two enforced anymore. Legacy-record backfill still deferred (no migration script; now also missing `email`). All tests passing (218/218). Awaiting Gate 2 code review. |
| portal-login-password-change | Portal Login and Password Change | In Development | developer.debayan@gmail.com | 2026-09-04 | v1.1 implemented: login field renamed `username`→`email` (T06). Separate `EMPLOYEE_JWT_SECRET` from Admin's; no token expiry (BRD-001 explicit "no session timeout", unlike admin-static-login's assumed 8h). |
| transfer-request-submission | Transfer Request Submission | In Development | developer.debayan@gmail.com | 2026-09-03 | New. Owns `TransferRequest` model + `transferRequestWorkflow.service.js` + shared `transferRequest.routes.js` (all 8 journey plans add routes to this one file) + `fulfillmentMessages` shared field. Escalation computed lazily on read, not a background job (no scheduler component in architecture.md). |
| current-manager-transfer-approval | Current Manager Transfer Approval | In Development | developer.debayan@gmail.com | 2026-09-03 | New. |
| current-hr-transfer-approval | Current HR Transfer Approval | In Development | developer.debayan@gmail.com | 2026-09-03 | New. Tenure computed from `Employee.createdAt` — flagged approximation. |
| receiving-hr-transfer-gatekeeping | Receiving HR Transfer Gatekeeping | In Development | developer.debayan@gmail.com | 2026-09-03 | New. Largest controller in the journey (8 handlers). Employee org-data update happens on gate-accept, before Receiving Manager approves — literal BRD reading, implemented as-is. |
| receiving-manager-transfer-approval | Receiving Manager Transfer Approval | In Development | developer.debayan@gmail.com | 2026-09-03 | New. Auto-hold on candidate-manager exhaustion (single code path covers both single- and multi-manager cases). |
| payroll-transfer-update | Payroll Transfer Update | In Development | developer.debayan@gmail.com | 2026-09-03 | New. Org-wide role-category scope, not Department/Location-scoped. |
| it-transfer-provisioning | IT Transfer Provisioning | In Development | developer.debayan@gmail.com | 2026-09-03 | New. Same structure as payroll-transfer-update. |
| facilities-transfer-arrangement | Facilities Transfer Arrangement | In Development | developer.debayan@gmail.com | 2026-09-03 | New. Same structure as payroll-transfer-update. |

## Daily Execution Log

### 2026-09-03
- Scaffolded `.agent/` and `.ai-context/` (ai-workspace-scaffold, Node.js/Express stack).
- Amended BRD-001–004 in BRD_Admin_Panel.md (Location↔Department mapping, Department↔Role mapping, Role `category` field, Employee Manager/HR mapping rules) and added BRD-005 (Static Admin Login).
- Drafted 5 specs (admin-static-login, location-crud, department-crud, role-crud, employee-crud-mapping).
- Ran Gate 1 on all five: reviewer independence and security/architecture sign-off confirmed by user; `code` uniqueness assumption and the employee `isActive` gap ratified; employee-crud-mapping's AC4/AC7 split to resolve bundled-behavior findings. All five now `Approved`.
- Drafted plan.md for all five specs (Node.js/Express/Mongoose). Resolved with user: new `/api/v1` route prefix (existing `/api/health` untouched) and JWT auth (8h expiry, flagged as an open assumption). Both `Location`/`Department`/`Role` deactivation guards and the Manager/HR deactivation cascade are explicitly re-deferred, not invented, pending BRD guidance. All five specs now `Plan Drafted`.
- Wrote `.ai-context/architecture.md` and `.ai-context/constitution.md` with real content (previously skeletons).
- Reviewed all five plans against the now-real `constitution.md`: added explicit test-first/Jest/80%-coverage steps to every plan's Sequencing, resolved Security Posture compliance per plan (PII list includes `name`; Employee plan's validation-error wording constraint called out specifically), confirmed `/api/v1` matches the now-formalized Versioning Rules. All five specs moved to `Plan Reviewed`.
- Generated tasks.md for all five plans (35 tasks total). Clarified that the three cross-plan deactivation guards (`LOCATION_HAS_ACTIVE_EMPLOYEES`/`DEPARTMENT_HAS_ACTIVE_EMPLOYEES`/`ROLE_HAS_ACTIVE_EMPLOYEES`) query the `employees` collection directly rather than importing Employee's model file, so they don't hard-depend on employee-crud-mapping's build order. All five specs now `Tasks Generated`.
- Implemented all 35 tasks across all five specs: `jsonwebtoken`/`jest`/`supertest` added; `src/app.js` split out from `src/index.js` for testability; all models (`Location`, `LocationDepartment`, `Department`, `DepartmentRole`, `Role`, `Employee`), `employeeMapping.service.js`, all five controllers, all five route files wired under `/api/v1`. 88 Jest tests, all passing, 92.27% line coverage project-wide (≥80% floor met on every plan's own files). Verified with a real end-to-end smoke test against the running MongoDB container (login + health check). All five specs now `In Development`, awaiting Gate 2 code review.
- Added BRD_Employee_Transfer.md#BRD-001–009 (Internal Transfer Request journey) as a linked BRD in BRD.md.
- Ran spec-generation for the Employee Transfer journey: user chose one spec per BRD entry (9 specs, matching the Admin Panel's 1:1 pattern). Resolved two structural gaps with the user before drafting: (1) portal login identity = the existing Employee record, amended in place rather than a new PortalAccount model; (2) Payroll/IT/Facilities modeled by extending Role's `category` enum, not as separate function-level accounts. Amended `role-crud.spec.md` (v1.1: category enum → HR/Manager/Payroll/IT/Facilities) and `employee-crud-mapping.spec.md` (v1.2: adds `code`-as-username, `passwordHash`, `mustChangePassword`, one-time `temporaryPassword` on creation) in place, both still in flight (Case A). Drafted 9 new specs: portal-login-password-change, transfer-request-submission (owns the shared `TransferRequest` status machine), current-manager-transfer-approval, current-hr-transfer-approval, receiving-hr-transfer-gatekeeping, receiving-manager-transfer-approval, payroll-transfer-update, it-transfer-provisioning, facilities-transfer-arrangement. Several BRD gaps filled with explicitly flagged assumptions rather than silent invention (tenure source, Payroll/IT applicability conditions, org-wide vs. Department-scoped fulfillment roles, the early organisational-data-update timing on Receiving HR's gate-accept) — all called out in each spec's Context section for Gate 1 to re-confirm. All 11 specs (2 amended, 9 new) added to the Active Specs table above, all `Draft`.
- Ran Gate 1 on all 11: reviewer independence and security/architecture sign-off confirmed by user (same pattern as the first 5 specs). One finding — `receiving-hr-transfer-gatekeeping.spec.md`'s API04/06/07/08 used condensed inline exception lists instead of the standard table format — fixed in place (v1.1). Bullet 7 (Related/Builds-on Approved/Released) judgment call: in-batch cross-references among these 11 co-approved specs were treated as passing, since the alternative would make an interdependent journey unable to clear Gate 1 together at all; flagged explicitly rather than silently passed. All 11 specs now `Approved`.
- Ran plan-generation for all 11 Approved specs. Revised the two existing Admin Panel plans in place: role-crud.plan.md (v1.1 enum widening) and employee-crud-mapping.plan.md (v1.2 credential fields — flagged an Open Question on bcrypt library choice, defaulted to `bcryptjs` with rationale; flagged legacy-record backfill as an unresolved deferred item). Drafted 9 new plans. Key cross-plan architecture decisions made explicit rather than left implicit: (1) escalation (2-day/5-business-day) computed lazily on every read rather than via a background scheduler, since constitution.md approves only synchronous REST and architecture.md has no scheduler component; (2) all 8 Internal Transfer journey plans share one `transferRequest.routes.js` file and one `fulfillmentMessages` log field, both declared by transfer-request-submission.plan.md specifically to avoid circular build-order dependencies between the three fulfillment plans; (3) Employee JWT gets no expiry (vs. Admin's assumed 8h), grounded in BRD-001's explicit "no session timeout" decision rather than copied from the sibling pattern. All 11 specs moved to `Plan Drafted`.
- User confirmed plan review for all 11 (same self-attested pattern as Gate 1) — all 11 specs moved to `Plan Reviewed`.
- Ran task-generation for all 11 Plan Reviewed plans. Appended revision tasks to the two existing Admin Panel tasks.md files (role-crud.T07; employee-crud-mapping.T10–T13), preserving all prior tasks' `Merged` checkbox state unchanged. Drafted 9 new tasks.md files (69 new tasks total across all 11 files' additions). AC-coverage check passed on every spec — every Acceptance Criteria ID has at least one citing task, including `transfer-request-submission.T03` (the shared `computeEscalation` utility), which has no AC of its own in that spec and is flagged as such, citing instead the six downstream specs' escalation ACs that depend on it. All 11 specs now `Tasks Generated`.

### 2026-09-04
- Implemented all tasks across all 11 specs sequentially, test-first, in dependency order (role-crud.T07 → employee-crud-mapping.T10–T13 → portal-login-password-change → transfer-request-submission → the remaining 7 journey specs). 70 new/updated Jest tests added (198 total project-wide, all passing, 94.43% line coverage — floor met on every file). Two implementation deviations from the plan text, both applied and documented inline in code comments rather than silently: (1) `Employee.passwordHash` got a fallback `default` (a throwaway bcrypt hash) alongside `required: true`, since ~15 pre-existing test fixtures create `Employee` documents directly without going through the create endpoint — the real create handler always overrides it, so production behavior is unaffected; (2) `TransferRequest.currentManagerId`/`currentHrId` were made nullable rather than `required: true` as the plan sketched, since a Manager- or HR-category Employee legitimately has a null `managerId`/`hrId` on their own record and the plan's schema didn't anticipate that valid data state. Confirmed via a debug script that Mongoose's timestamps plugin strips `createdAt` from `Model.updateOne()` by design (immutable via updates) — not a bug, just meant test fixtures backdating tenure had to use the raw driver collection. `role-crud`, `employee-crud-mapping`, and all 9 new specs moved to `In Development`, awaiting Gate 2 code review. Not yet done: an end-to-end smoke test against the running Docker Mongo container (only run against the Jest test DB so far).
- Added Swagger/OpenAPI docs for all 25 new endpoints (23 in `transferRequest.routes.js`, 2 in `auth.routes.js`), grounded in each spec's API Contract. Added a second `employeeBearerAuth` security scheme to `src/config/swagger.js` (distinct from the existing Admin `bearerAuth`) and a shared `TransferRequest` schema component referenced via `$ref` across all 23 transfer-request blocks rather than repeating the full shape each time. Verified live: `GET /api-docs` loads (200 after redirect), `GET /api-docs.json` lists all 25 new paths, and spot-checked error responses (`INVALID_CREDENTIALS`, `UNAUTHORIZED`) against the running server match what's documented. Full Jest suite still green (198/198) after the doc changes.
- User-directed change: removed `code` from the Employee create/update contract entirely, replacing it with `email` (unique identifier + portal login field) and an Admin-supplied `password` (replacing the v1.2 system-generated `temporaryPassword` mechanism, which existed only to solve a delivery-channel problem that no longer exists once the Admin sets the password directly). Amended employee-crud-mapping.spec.md (v1.3) and portal-login-password-change.spec.md (v1.1, `username`→`email`) plus both plans and tasks.md files, all Case A in-place edits (still `In Development`, not Released) — flagged as a breaking API change not requiring a `/api/v2` bump only because there are no external consumers yet, per constitution.md's own Versioning Rules reasoning. Deleted `src/utils/generatePassword.js` (dead code). Dropped the stale `code_1` unique index from both the dev and test `employees` collections (Mongoose's `autoIndex` never removes indexes no longer in the schema — without this, every create after the field removal would collide on `code: null`). Updated all 16 affected test files (~35 Employee fixtures) and both route files' Swagger docs. **Incident, caught and fixed same-turn:** while cleaning up a live-smoke-test employee, ran `db.departmentroles.deleteMany({})` against the dev database instead of a scoped delete, wiping all 6 of the India demo data's Department↔Role mappings seeded earlier in this session; reconstructed and verified all 6 immediately after. Full suite: 199/199 passing, 94.4% coverage. Verified live end-to-end (create Employee with email/password as Admin → login as that Employee) against the real Docker Mongo container.
- User-directed change: added real hard delete (`DELETE /api/v1/<resource>/:id`, 204/irreversible) for Location, Department, Role, and Employee, additive alongside the existing soft-delete (`PATCH .../status`). Amended `BRD_Admin_Panel.md` (BRD-001–004) and all 4 specs/plans/tasks.md in place. Guard design: Location/Department/Role block deletion if any Employee (active *or* inactive, deliberately broader than the existing active-only deactivation guards) still references them, and cascade-delete their own join-table mappings (LocationDepartment/DepartmentRole) on success since those carry no independent meaning once the parent is gone; Employee blocks deletion if referenced as another Employee's `managerId`/`hrId` (new `EMPLOYEE_HAS_DEPENDENTS` guard, doesn't exist for deactivation) and cascades nothing. Flagged, not resolved: hard-deleting an Employee doesn't check `TransferRequest` references — out of the requested Admin-Panel-CRUD scope. 14 new backend tests added (219 total, all passing, 94.64% coverage). Verified live via curl against the real Docker Mongo container (create → delete → 404 on subsequent GET).
- User reported the Admin Panel's Create Employee form showing a blank Role dropdown. Root cause: after the DB was fully wiped and reseeded (2 Locations, 2 Departments — user declined adding the third "Business Partner" Department, 3 Roles), no `DepartmentRole` enablement mappings were recreated for any Department, so `department-crud.API08`'s "Roles enabled for this Department" query returned empty for every Department, and the Admin UI's Role selector (scoped to enabled Roles) had nothing to show. User's directed resolution, confirmed via AskUserQuestion: remove the restriction only — any active Role may now be assigned to an Employee in any Department; the Department↔Role "Enable/Disable Role" feature itself (department-crud.API08, its mapping panel) is kept as-is, just no longer consulted by Employee create/update. Amended `BRD_Admin_Panel.md` (BRD-004) and `employee-crud-mapping.spec.md`/`plan.md`/`tasks.md` (v1.4) in place — AC11/UT11 marked removed (not renumbered, to avoid churning every other AC/UT/task cross-reference in this still-`In Development` spec). Removed `validateDepartmentRoleEnablement` and the now-unused `DepartmentRole` import from `employeeMapping.service.js`; removed the `ROLE_NOT_ENABLED_FOR_DEPARTMENT` 409 case from `employee.routes.js`'s Swagger docs; removed the corresponding test coverage and now-unnecessary `DepartmentRole` seeding from `employeeMapping.service.test.js`/`employee.controller.test.js`/`employee.routes.test.js`. `transferRequest.controller.js`'s own independent Department↔Role enablement check (a separate, non-shared implementation backing `transfer-request-submission.spec.md`'s AC5) was deliberately left untouched — different feature, out of this change's requested scope. Full suite: 218/218 passing (net −1 from the removed AC11 test), `--runInBand` (matches the `npm test` script; a parallel run against the shared real test-Mongo instance produces unrelated cross-suite races and is not a valid way to run this suite).
