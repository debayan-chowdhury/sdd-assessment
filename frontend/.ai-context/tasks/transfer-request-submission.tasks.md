# Tasks: Transfer Request Submission

## Derived From
.ai-context/plans/transfer-request-submission.plan.md (spec Status: Draft, plan not `Plan Reviewed` — Gate 1/plan-review skipped by explicit user direction, 2026-09-04. These tasks are speculative until the spec/plan are actually reviewed.)

## Sequence
- [x] transfer-request-submission.T01 — Create `src/types/transferRequest.ts` (`TransferRequestStatus`, `FulfillmentSubStatus`, `TransferRequest`) — Acceptance: AC10
- [x] transfer-request-submission.T02 — Create `src/features/transfer-request/transfer-request.api.ts` (API01 submit, API02 list-mine, API03 get-by-id) and `transfer-request.queries.ts` (`useMyTransferRequests`, `useTransferRequest`) — Acceptance: AC10, AC13
- [x] transfer-request-submission.T03 — Create `transfer-request.mutations.ts` with `useSubmitTransferRequest` (API01) — Acceptance: AC2
- [x] transfer-request-submission.T04 — Build `src/screens/transfer-request/components/` — status timeline, `RejectionBanner` (rejection reason), `HoldBanner` (hold reason + 6-month reopen explanation), `EscalationIndicator` — with tests — Acceptance: AC11, AC12, AC14
- [x] transfer-request-submission.T05 — Build `src/screens/transfer-request/NewTransferRequestScreen.tsx` — required-field validation, client-side 30-day effective-date validation (including disabling dates within the window), submits via `useSubmitTransferRequest`, maps 409 `ACTIVE_REQUEST_EXISTS` / 404 `NOT_FOUND` / 409 `ROLE_NOT_ENABLED_FOR_DEPARTMENT` / 404 `NO_RECEIVING_HR` to messages, redirects to the status view on success — and `src/app/transfer-request/new/page.tsx`, with tests — Acceptance: AC2, AC3, AC4, AC6, AC7, AC8, AC9. **Open Question resolved (2026-09-04)**: added new Employee-token-accessible `GET /api/v1/options/{locations,departments,roles}` endpoints (`backend/src/routes/options.routes.js`, `employeeAuth`-gated, active-only) since the journey's own contract had no such endpoint and the Admin Panel's CRUD endpoints are admin-only. `useTransferRequestOptions` now calls these for real via `useLocationOptions`/`useDepartmentOptions`/`useRoleOptions` (`transfer-request.queries.ts`); on 404 `NOT_FOUND` the screen also refetches the option lists per AC7. **Amended same day (AC15)**: Department is now cascaded by Location — `useDepartmentOptions(locationId)` only fetches once a Location is picked, `GET /api/v1/options/departments` requires `locationId`, the Department `SelectField` is disabled with a "Select a location first" placeholder until then, and `handleLocationChange` clears any selected Department when the Location changes. Role stays a flat, unfiltered list. Tested in `NewTransferRequestScreen.test.tsx`.
- [x] transfer-request-submission.T06 — Build `src/screens/transfer-request/TransferRequestStatusScreen.tsx` — renders the user's request(s) newest-first using the T04 components, including per-target sub-status display and 403-as-not-found handling — and `src/app/transfer-request/[id]/page.tsx`, with tests — Acceptance: AC10, AC11, AC12, AC13, AC14
- [x] transfer-request-submission.T07 — Build `src/app/transfer-request/page.tsx` — resolves to `NewTransferRequestScreen` or redirects to the active request's status view, based on `useMyTransferRequests` — Acceptance: AC1, AC5. **Amended (2026-09-05, direct user request)**: `TransferRequestHomeScreen` no longer redirects to `/transfer-request/[id]`. It now renders, on `/transfer-request` itself: the employee's own profile (`EmployeeProfileSummary`, via a new shared `useEmployeeProfileDisplay` hook also used by the dashboard), a "My transfer requests" section listing **every** request the employee has ever submitted (not just the active one — extracted the per-request status card into `TransferRequestSummaryCard`, reused by the dashboard), and the new-request form only when no non-terminal request exists. Still satisfies AC5 ("status view is shown directly," now literally in-place rather than via redirect) and AC1.

## AC Coverage Check
AC1 (T07) · AC2 (T03, T05) · AC3 (T05) · AC4 (T05) · AC5 (T07) · AC6 (T05) · AC7 (T05) · AC8 (T05) · AC9 (T05) · AC10 (T01, T02, T06) · AC11 (T04, T06) · AC12 (T04, T06) · AC13 (T02, T06) · AC14 (T04, T06) · AC15 (T05) — all 15 covered.

## Open Item Carried Forward
Resolved 2026-09-04 — see T05 note. New backend endpoints (`GET /api/v1/options/locations|departments|roles`) are outside this journey's original spec/plan contract and are now documented in their own backend spec/plan/tasks trio: `backend/.ai-context/specs/employee-transfer/transfer-options.spec.md` (+ `plans/`, `tasks/`).
