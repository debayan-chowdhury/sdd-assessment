# Tasks: Receiving HR Transfer Gatekeeping

## Derived From
.ai-context/plans/receiving-hr-transfer-gatekeeping.plan.md (spec Status: Draft, plan not `Plan Reviewed` — Gate 1/plan-review skipped by explicit user direction, 2026-09-04. These tasks are speculative until the spec/plan are actually reviewed.)

## Sequence
- [x] receiving-hr-transfer-gatekeeping.T01 — Extend `src/types/transferRequest.ts` with `NeedInformationReply` (`{ target: "payroll" | "it" | "facilities", message: string }`) — Acceptance: AC8
- [x] receiving-hr-transfer-gatekeeping.T02 — Extend `transfer-request.queries.ts` with `useReceivingHrGateQueue` (API01) and `useReceivingHrFulfillmentQueue` (API05) — Acceptance: AC1, AC6
- [x] receiving-hr-transfer-gatekeeping.T03 — Extend `transfer-request.mutations.ts` with `useReceivingHrGateDecision` (API02) and `useReassignManager` (API03), invalidating the gate queue on success, surfacing 400 `INVALID_MANAGER_ROLE` / 403 / 404 / 409 — Acceptance: AC3, AC4, AC5, AC12
- [x] receiving-hr-transfer-gatekeeping.T04 — Extend `transfer-request.mutations.ts` with `useTriggerFulfillment` (API04), `useRespondNeedInformation` (API06), `useConfirmCompletion` (API07), invalidating the fulfillment queue on success — Acceptance: AC7, AC8, AC10
- [x] receiving-hr-transfer-gatekeeping.T05 — Extend `transfer-request.mutations.ts` with `useReopenHold` (API08), surfacing 409 `HOLD_WINDOW_EXPIRED` — Acceptance: AC13, AC14
- [x] receiving-hr-transfer-gatekeeping.T06 — Build `src/components/ui/ManagerPicker.tsx` (required-selection control) against `useCandidateManagers(locationId, departmentId)` — Acceptance: AC2. **Open Question resolved (2026-09-05)**: added a new Employee-token-accessible `GET /api/v1/options/managers` endpoint (`backend/src/controllers/options.controller.js#listManagers`, mirrors `transferRequestWorkflow.service#findCandidateManagers`), same pattern as transfer-request-submission.T05's `/options/{locations,departments,roles}`. `ManagerPicker` now calls it for real via `useCandidateManagers`.
- [x] receiving-hr-transfer-gatekeeping.T07 — Build `src/screens/approvals/ReceivingHrGateQueue.tsx` (list, Accept using `ManagerPicker` + `useReceivingHrGateDecision`, Reject via `RejectReasonDialog` in optional mode, Reassign action using `ManagerPicker` + `useReassignManager`), composed into `ApprovalsInboxScreen`, with tests — Acceptance: AC1, AC2, AC3, AC4, AC5, AC12, AC15, AC16, AC17. **Amended (2026-09-05)**: the Reassign action's list view also needed a backend list endpoint that didn't exist (`status: "Pending Receiving HR Reassignment"`, scoped to `receivingHrId`) — added `GET /transfer-requests/pending/receiving-hr-reassignment` (`receivingHrGatekeeping.controller.js#listReassignmentPending`) and `useReceivingHrReassignmentQueue`; rendered as a second "Reassignment Needed" section within this screen.
- [x] receiving-hr-transfer-gatekeeping.T08 — Build `src/screens/approvals/ReceivingHrFulfillmentQueue.tsx` (list including Not Applicable items, Trigger Fulfillment button, Need Information reply control with required-message validation, Confirm Completion button disabled until every applicable sub-status is Done), composed into `ApprovalsInboxScreen`, with tests — Acceptance: AC6, AC7, AC8, AC9, AC10, AC11, AC15, AC16, AC17
- [x] receiving-hr-transfer-gatekeeping.T09 — Build `src/screens/approvals/ReceivingHrHoldReopen.tsx` (Reopen action using `ManagerPicker` + `useReopenHold`, surfaces the expired-window message), rendered on any `Hold` request where the caller is `receivingHrId`, with tests — Acceptance: AC13, AC14. **Amended (2026-09-05)**: same class of gap as T07's reassignment queue — added `GET /transfer-requests/pending/receiving-hr-hold` (`receivingHrGatekeeping.controller.js#listHoldPending`) and `useReceivingHrHoldQueue` to list the caller's own Hold requests.

## AC Coverage Check
AC1 (T02, T07) · AC2 (T06, T07) · AC3 (T03, T07) · AC4 (T03, T07) · AC5 (T03, T07) · AC6 (T02, T08) · AC7 (T04, T08) · AC8 (T01, T04, T08) · AC9 (T08) · AC10 (T04, T08) · AC11 (T08) · AC12 (T03, T07) · AC13 (T05, T09) · AC14 (T05, T09) · AC15 (T07, T08) · AC16 (T07, T08) · AC17 (T07, T08) — all 17 covered.

## Open Item Carried Forward
Resolved 2026-09-05 — see T06/T07/T09 notes. All three new backend endpoints (`GET /options/managers`, `GET /transfer-requests/pending/receiving-hr-reassignment`, `GET /transfer-requests/pending/receiving-hr-hold`) are outside this journey's original spec/plan contract, same as transfer-request-submission's `/options/*` precedent — not yet captured in their own backend spec/plan/tasks trio the way that one was.
