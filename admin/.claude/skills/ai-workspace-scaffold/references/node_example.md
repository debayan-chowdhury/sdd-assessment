# Worked example — Node.js — for calibration only

This is the reference pattern the stack guide's structure was distilled from.
Use it to gauge the right level of specificity, not as copy-paste content for
a different stack.

## `.agent/rules/int-standards.node.md`

```markdown
# INT Node.js Engineering Standards

You are assisting an engineering team at INT. Whenever you write, refactor,
or suggest Node.js code, you must strictly adhere to the following rules:

## 1. Code Style and Structure
* Use ES6+ syntax consistently. Prefer `const` and `let` over `var`.
* Always use `async/await` for asynchronous operations. Do not use raw
  Promises (`.then().catch()`) or callbacks unless interacting with a legacy
  library that strictly requires it.
* Keep functions small and modular. A single function should do one thing well.
* Use descriptive variable and function names. Avoid single-letter variables
  except in standard loops (like `i`, `j`).

## 2. Error Handling
* Wrap all asynchronous operations in `try/catch` blocks.
* Never swallow errors. Always log the error details using the project's
  standard logger (e.g. Winston or Pino), not just `console.log`.
* Return standard HTTP status codes and structured JSON error responses for
  API endpoints.

## 3. Security
* Never hardcode sensitive information like API keys, database credentials,
  or secrets. Always read these from environment variables (`process.env`).
* Validate and sanitize all incoming user payload data before processing it.
* Avoid using standard `eval()` or executing arbitrary code.

## 4. Performance
* Prevent event loop blocking. Offload heavy synchronous computations to
  worker threads or external services.
* Ensure database queries are optimized and use indexing where appropriate.

## 5. Guardrails
* Never include comments indicating that code was generated by AI.
* Never write default commit messages that give hints of AI-generated code.
* Do not add any AI-specific configuration files (like `.cursorrules` or
  `.copilotignore`) outside of the `.agent/` directory.
```

## `.agent/rules/.agentignore`

```
# ==========================================
# INT Node.js AI Agent Ignore File
# ==========================================

# 1. Heavy Dependencies (Massive token sinks)
node_modules/
package-lock.json
yarn.lock
pnpm-lock.yaml

# 2. Build Artifacts & Caches (Compiled code is useless for AI context)
dist/
build/
out/
.next/
.nuxt/
coverage/
.cache/

# 3. Environment Variables & Secrets (Security boundaries)
.env
.env.*
*.pem
*.key
credentials.json
secrets/

# 4. Logs & Local Databases (Noise that confuses the AI)
logs/
*.log
npm-debug.log*
*.sqlite
*.db

# 5. Media & Static Assets (Unreadable binary data)
public/assets/
images/
*.svg
*.png
*.jpg
*.mp4
*.ico

# 6. AI Audit Trail (Prevents recursive reading)
.ai-context/prompt_history.md
```

## `.agent/workflows/code-review.md`

```markdown
# Node.js Code Review Workflow

Please act as a Senior Node.js Technical Lead and review the provided code.
Do not rewrite the entire file immediately. Instead, analyze the code and
provide constructive feedback categorized under the following headers:

## 1. Security Vulnerabilities
Identify any risks such as SQL injection, Cross-Site Scripting (XSS),
insecure direct object references, or exposed secrets.

## 2. Performance Bottlenecks
Highlight any code that might block the Node.js event loop, cause memory
leaks, or result in inefficient database queries (like N+1 problems).

## 3. Reliability & Error Handling
Check if the code properly handles edge cases, network failures, and
unexpected null/undefined values. Ensure all async functions have proper
`try/catch` coverage.

## 4. Maintainability
Point out overly complex functions, tight coupling, or violations of the
DRY (Don't Repeat Yourself) principle. Suggest better naming conventions
if applicable.

After providing your analysis, offer specific, isolated code snippets
showing how to fix the most critical issues you found.
```

## `.agent/workflows/generate-tests.md`

```markdown
# Unit Test Generation Workflow

Please generate unit tests for the provided Node.js code. You must adhere to
the following testing standards:

* **Framework:** Assume we are using Jest as our primary testing framework.
* **Coverage:** Write tests covering the "happy path", expected failure
  scenarios, and edge cases.
* **Mocking:** Mock all external dependencies, including database calls,
  third-party API requests, and file system operations using Jest's mocking
  utilities.
* **Assertions:** Write clear and descriptive assertions. Ensure that the
  error messages actually test the specific error thrown, not just a
  generic failure.
* **Structure:** Group related tests using `describe` blocks and use `it`
  blocks for individual test cases. Follow the Arrange-Act-Assert pattern.

Output the complete test file code so it can be copied directly into the
project.
```
