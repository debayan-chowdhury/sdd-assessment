# Project Status Board

_Last updated: 2026-09-04_

## Active Specs

| Spec ID | Title | Status | Owner | Last Updated | Notes |
|---|---|---|---|---|---|
| admin-static-login | Static Admin Login (UI) | Changes Requested ⟲ | Unassigned | 2026-09-03 | Gate 1: needs security/architecture sign-off (auth feature); API Contract needs full per-endpoint detail; blocks the other four via Related. All 5 tasks (T01–T05) implemented in the working tree per the user's explicit request — real code exists ahead of the formal gate chain. Spec Status deliberately left at Changes Requested ⟲ (not advanced) since none of the underlying gates actually cleared. |
| location-crud | Location CRUD (UI) | Changes Requested ⟲ | Unassigned | 2026-09-04 | Gate 1: API Contract needs full per-endpoint detail; Related specs (admin-static-login etc.) not yet Approved. Speculative plan.md and tasks.md drafted (bypass gate, not official). T10-T11 implemented: real hard "Delete" (irreversible), `ConfirmDialog` gains `variant="danger"`. |
| department-crud | Department CRUD (UI) | Changes Requested ⟲ | Unassigned | 2026-09-04 | Gate 1: API Contract needs full per-endpoint detail; Related specs not yet Approved. Speculative plan.md and tasks.md drafted (bypass gate, not official). T09 implemented: real hard "Delete" (irreversible). |
| role-crud | Role CRUD (UI) | Changes Requested ⟲ | Unassigned | 2026-09-04 | Gate 1: API Contract needs full per-endpoint detail; Related specs not yet Approved. Speculative plan.md and tasks.md drafted (bypass gate, not official). T08 implemented: real hard "Delete" (irreversible). |
| employee-crud-mapping | Employee CRUD and Manager/HR Mapping (UI) | Changes Requested ⟲ | Unassigned | 2026-09-04 | Gate 1: needs security/architecture sign-off (employee PII); API Contract needs full per-endpoint detail; Related specs not yet Approved. T10-T13 implemented (`code`→`email`, Admin-set `password` on create) tracking backend v1.3. T14 implemented: real hard "Delete" (irreversible). T15 (v1.1) implemented: Role selector no longer scoped to Roles enabled for the selected Department — Role/Department carry no relation now, tracking backend v1.4. `tsc`/`lint`/`build` all clean. |

## Daily Execution Log

### 2026-09-03
- admin-static-login, location-crud, department-crud, role-crud, employee-crud-mapping: five frontend specs drafted, mirroring the five backend specs 1:1 (same BRD entries BRD-001–005).
- All 5 ran through Gate 1 review same day. Outcome: Changes Requested ⟲ for all 5 — structural blockers (Related specs not yet Approved, forming a cycle since all 5 reference each other; reviewer independence not yet satisfied — this review was self-authored, capping any spec below Approved regardless of content) plus real content gaps (API Contract sections need full per-endpoint payload/response/exceptions instead of the summary table used; admin-static-login and employee-crud-mapping additionally need security/architecture sign-off). Recommended remediation order: fix and re-review admin-static-login first (no Related dependencies), then the other four in sequence, with an independent human reviewer and the two sign-offs obtained before any can reach Approved.
- At the user's explicit request, `plan.md` was drafted for all 5 specs anyway, bypassing the still-unresolved Gate 1 blockers. Each plan is marked speculative/non-official at the top of the file. Spec `## Status` fields were deliberately **not** advanced to `Plan Drafted` (per plan-generation's bypass rule) — they remain `Changes Requested ⟲` until actually revised and re-reviewed. Sequencing note: admin-static-login's plan builds the shared `axios.ts`/`queryClient.ts`/`AuthGuard`; location-crud's plan builds the first shared `components/ui/` primitives (DataTable/Modal/ConfirmDialog/StatusBadge); each of location→department→role's plans pulls the next entity's minimal read-query hook forward to power its own cross-entity selector, so the real build order is admin-static-login → location-crud → department-crud → role-crud → employee-crud-mapping.
- At the user's explicit request, `tasks.md` was also drafted for all 5, decomposing each plan's Sequencing into 5–9 independently-verifiable, AC-mapped tasks (38 tasks total: T01–T05 login, T01–T09 location, T01–T08 department, T01–T07 role, T01–T09 employee). Every Acceptance Criteria ID across all 5 specs is covered by at least one task — confirmed in each file's own AC Coverage section. Spec `## Status` fields were again deliberately **not** advanced to `Tasks Generated` (bypass rule) — they remain `Changes Requested ⟲`.
- admin-static-login.T01–T05 implemented at the user's explicit request (`src/lib/{axios.ts,queryClient.ts}`, `src/app/providers.tsx`, `src/types/auth.ts`, `src/features/auth/{auth.api.ts,auth.store.ts,auth.mutations.ts}`, `src/components/layout/AuthGuard.tsx`, `src/screens/login/LoginPage.tsx`, `src/app/login/page.tsx`), dependencies added (`axios@^1.20.0`, `@tanstack/react-query@^5.102.8`, `zustand@^5.0.15`). `yarn lint`/`tsc --noEmit`/`yarn build` all clean; backend round-trip verified via curl against the real running backend (Mongo + Express on :4000). Browser visual check not done — Chrome extension was disconnected. **Real architectural correction surfaced during implementation**: `src/pages/` (used throughout every spec/plan/tasks file) collides with Next.js's reserved Pages Router directory and broke the build — renamed project-wide to `src/screens/`, including in `architecture.md` and `constitution.md` (see architecture.md's Known Constraints for the permanent note). `tasks.md` checkboxes for T01–T05 marked done; spec `## Status` intentionally left at `Changes Requested ⟲`.

### 2026-09-04
- User reported the running dev server's Employee create form (localhost:3000/employees) was not asking for a password, and the backend's employee-crud-mapping API had meanwhile been changed (in the sibling backend project) to remove `code` in favor of `email` + an Admin-set `password` (v1.3). Updated this project to match: `src/types/employee.ts` (`Employee`/`EmployeeInput` `code`→`email`; new `EmployeeCreateInput` adding `password`, POST-only), `employees.api.ts` (`EmployeeDto`), `employees.mutations.ts` (create mutation now typed on `EmployeeCreateInput`), `EmployeeFormModal.tsx` (email field replaces code field; password field added, shown only in create mode; submit split into separate create/update calls so update never sends a password), `EmployeeMappingFields.tsx`/`EmployeeTable.tsx`/`EmployeeDetailPage.tsx` (display labels `code`→`email`). `employee-crud-mapping.spec.md`/`plan.md`/`tasks.md` (T10–T13) updated in place to match — spec was never `Approved` (still `Changes Requested ⟲`), so this is a direct content edit, not a versioned amendment. `tsc --noEmit`, `yarn lint`, and `yarn build` all clean. Did not restart the user's already-running `next dev` (:3000) or backend (:4000) processes — Fast Refresh should pick up the source changes; user may need to reopen the form if it doesn't.
- User asked to add a real Delete option (Location/Department/Role/Employee) in both this project and the sibling backend, and to update specs/plans/tasks. Clarified via AskUserQuestion: real hard delete (irreversible), not just surfacing the existing soft-delete toggle — this reverses each spec's prior "hard delete never available in this UI" Out-of-Scope bullet. Backend implemented first (new `DELETE /api/v1/{locations,departments,roles,employees}/:id`, each with a referential-integrity guard: Location/Department/Role check ALL employees, active and inactive, and cascade-delete pure join-table records i.e. LocationDepartment/DepartmentRole; Employee checks `managerId`/`hrId` references, does not cascade, and does not check TransferRequest references — a documented gap, out of this scope). All 4 specs, plans, and tasks.md amended here to match: new AC/UT pairs (location AC15/16, department AC15/16, role AC16/17, employee AC16/17), new API Contract row, new error codes (`LOCATION_HAS_EMPLOYEES`/`DEPARTMENT_HAS_EMPLOYEES`/`ROLE_HAS_EMPLOYEES`/`EMPLOYEE_HAS_DEPENDENTS`). Shared `ConfirmDialog.tsx` gained a `variant?: "default" | "danger"` prop (red confirm button) — added once by location-crud's plan/tasks (T10), reused by the other three. Each entity's Detail page got a "Delete" button + `ConfirmDialog variant="danger"`, wired to a new `useDelete<Entity>Mutation`, rendering the entity-specific 409 error inline and navigating back to the list screen on success (`EmployeeDetailPage.tsx` additionally needed `getApiErrorCode` added to its existing error-helper imports). `tsc --noEmit`, `yarn lint`, `yarn build` all verified clean across all 4 changes.
- User reported the Create Employee form's Role dropdown showing blank at `localhost:3000/employees`. Root cause: after the DB was fully wiped and reseeded (2 Locations, 2 Departments — user declined adding the third "Business Partner" Department when asked, 3 Roles), no `DepartmentRole` enablement mappings were recreated for any Department, so the Role selector (scoped to Roles enabled for the selected Department, per `department-crud.API08`) had nothing to show for any Department. Confirmed via AskUserQuestion: remove the restriction only — the Role selector now sources from all active Roles (`useRolesQuery({ isActive: true })`) regardless of Department; the Department↔Role "Enable/Disable Role" feature itself (its mapping panel on the Department detail screen, `department-crud.API08`) is kept as-is, just no longer consulted by Employee create/update. `EmployeeMappingFields.tsx` updated: swapped `useDepartmentRolesQuery(departmentId)` for `useRolesQuery({ isActive: true })`, removed the Role `<select>`'s `disabled={!departmentId}` gating. `employee-crud-mapping.spec.md` (v1.1), `.plan.md`, `.tasks.md` (T15) amended in place — AC9/UT08 marked removed (not renumbered, matching the same reasoning used for the backend's AC11/UT11 removal). Also discovered and fixed a pre-existing gap while in this file: `BRD_Admin_Panel.md` here had silently fallen out of sync with the backend project's copy — missing all four entities' hard-delete amendments from the prior session — brought back in sync, plus the new no-relation amendment added to both. `tsc --noEmit`, `yarn lint`, `yarn build` all clean.
